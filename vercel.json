{
  "buildCommand": "npm ci && npm rebuild esbuild || true && npm rebuild lightningcss || true && chmod +x node_modules/.bin/vite && vite build",
  "outputDirectory": "dist",
  "installCommand": "npm ci",
  "routes": [
    { "src": "/[^.]+", "dest": "/", "status": 200 }
  ]
}
